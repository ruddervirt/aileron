apiVersion: ruddervirt.io/v1alpha1
kind: VirtualMachineBuild
metadata:
  name: base-linux
  namespace: ruddervirt-system
spec:
  network:
    subnets:
      - name: lan
        cidr: "10.0.0.0/24"
  files:
    - name: preseed.cfg
      inline: |
        # To see all available options execute this command once the install is done:
        # sudo less /var/log/installer/cdebconf/questions.dat
        # If you need information about an option use the command below (example for keymap):
        # grep -A 4 "keyboard-configuration/xkb-keymap" /var/log/installer/cdebconf/templates.dat

        # Use network mirror for package installation
        # d-i apt-setup/use_mirror boolean true

        # Automatic installation
        d-i auto-install/enable boolean true

        # "linux-server" is substituted by "linux-image-amd64"
        # Possible options : "linux-image-amd64"(default) or "linux-image-rt-amd64"
        d-i base-installer/kernel/override-image string linux-server

        # Configure hardware clock
        d-i clock-setup/utc boolean true
        d-i clock-setup/utc-auto boolean true

        # d-i console-setup/ask_detect boolean false

        # d-i debconf/frontend select noninteractive

        # Set OS locale
        d-i debian-installer/language string en
        d-i debian-installer/country string US
        d-i debian-installer/locale string en_US.UTF-8

        # d-i debian-installer/framebuffer boolean false

        # Reboot once the install is done
        d-i finish-install/reboot_in_progress note

        # Bootloader options
        d-i grub-installer/only_debian boolean true
        d-i grub-installer/with_other_os boolean true
        d-i grub-installer/bootdev string /dev/vda

        # Set the keyboard layout
        d-i keyboard-configuration/xkb-keymap select us

        # Mirror from which packages will be downloaded
        d-i mirror/country string manual
        d-i mirror/http/hostname string deb.debian.org
        d-i mirror/http/directory string /debian

        # Configure http proxy if needed "http://[[user][:pass]@]host[:port]/"
        d-i mirror/http/proxy string

        # Disk configuration
        d-i partman-efi/non_efi_system boolean true
        d-i partman-auto-lvm/guided_size string max
        d-i partman-auto/choose_recipe select atomic
        d-i partman-auto/method string lvm
        d-i partman-lvm/confirm boolean true
        d-i partman-lvm/confirm_nooverwrite boolean true
        d-i partman-lvm/device_remove_lvm boolean true
        d-i partman/choose_partition select finish
        d-i partman/confirm boolean true
        d-i partman/confirm_nooverwrite boolean true
        d-i partman/confirm_write_new_label boolean true

        # User configuration
        d-i passwd/root-login boolean false
        d-i passwd/user-fullname string skills
        d-i passwd/user-uid string 1000
        d-i passwd/user-password password skills
        d-i passwd/user-password-again password skills
        d-i passwd/username string skills

        # Extra packages to be installed
        d-i pkgsel/include string sudo qemu-guest-agent

        d-i pkgsel/install-language-support boolean false
        d-i pkgsel/update-policy select none

        # Whether to upgrade packages after debootstrap
        d-i pkgsel/upgrade select full-upgrade

        # Set timezone
        d-i time/zone string America/Chicago

        # Allow weak user password
        d-i user-setup/allow-password-weak boolean true

        # Home folder encryption
        d-i user-setup/encrypt-home boolean false

        # Do not scan additional CDs
        apt-cdrom-setup apt-setup/cdrom/set-first boolean false

        # Use network mirror
        apt-mirror-setup apt-setup/use_mirror boolean true

        # Disable polularity contest
        popularity-contest popularity-contest/participate boolean false

        # Select base install
        tasksel tasksel/first multiselect standard, ssh-server

        # Setup passwordless sudo for packer user
        d-i preseed/late_command string \
          in-target rm -rf /var/lib/apt/lists ; \
          in-target apt-get update ; \
          in-target apt-get -y upgrade ; \
          in-target apt-get -y dist-upgrade ; \
          in-target apt-get -y install vim systemd-resolved ; \
          in-target apt-get -y autoremove ; \
          in-target apt-get autoclean ; \
          in-target apt-get clean ; \
          echo "%sudo ALL=(ALL:ALL) NOPASSWD:ALL" > /target/etc/sudoers.d/passwordless && chmod 0440 /target/etc/sudoers.d/passwordless
    - name: dhcp.network
      inline: |
        [Match]
        Name=en*

        [Network]
        DHCP=yes
        UseDomains=true

        [DHCPv4]
        UseDomains=true
    - name: getty.sh
      inline: |
        #!/bin/bash
        # setup-serial-console.sh
        # Run with sudo or as root

        # only allow 1 virtual terminal
        sed -i 's/^#NAutoVTs=.*/NAutoVTs=1/' /etc/systemd/logind.conf

        # Add serial console to GRUB
        if ! grep -q "console=ttyS0" /etc/default/grub; then
          # Backup grub config
          cp /etc/default/grub /etc/default/grub.bak

          # Update GRUB parameters
          sed -i 's/GRUB_CMDLINE_LINUX_DEFAULT=".*"/GRUB_CMDLINE_LINUX_DEFAULT="console=tty0 console=ttyS0,115200n8"/' /etc/default/grub
          echo 'GRUB_TERMINAL="console serial"' >> /etc/default/grub
          echo 'GRUB_SERIAL_COMMAND="serial --speed=115200 --unit=0 --word=8 --parity=no --stop=1"' >> /etc/default/grub

          # Update GRUB
          update-grub
        fi

        # Enable serial getty service
        systemctl enable serial-getty@ttyS0.service

        # Configure auto-login for root
        mkdir -p /etc/systemd/system/serial-getty@ttyS0.service.d/
        cat > /etc/systemd/system/serial-getty@ttyS0.service.d/autologin.conf << EOF
        [Service]
        ExecStart=
        ExecStart=-/sbin/agetty --autologin root --noclear %I \$TERM
        EOF

        # Reload systemd
        systemctl daemon-reload

        echo "Serial console setup complete. After reboot, connect using: virsh console <vm-name>"
    - name: nsswitch.sh
      inline: |
        #!/bin/bash
        sed -i 's/\[!UNAVAIL=return\]//g' /etc/nsswitch.conf
    - name: ruddergrade
      url: "https://github.com/ruddervirt/ruddergrade/releases/download/v1.0.0/ruddergrade"
  httpDirectory:
    files:
      - name: preseed.cfg
  vms:
    - name: base
      source:
        blank: true
      resources:
        cpu: 2
        memory: "4Gi"
      disks:
        - name: rootdisk
          size: "25Gi"
      isos:
        - url: "https://cdimage.debian.org/cdimage/archive/12.9.0/amd64/iso-cd/debian-12.9.0-amd64-netinst.iso"
          checksum: "1257373c706d8c07e6917942736a865dfff557d21d76ea3040bb1039eb72a054"
      communicator:
        sshUsername: skills
        sshPassword: skills
        sshTimeout: 30m
      nics:
        - name: eth0
          subnet: lan
          ip: "10.0.0.10"
      efiFirmware:
        secureBoot: false
      bootCommand:
        # Wait for EFI GRUB menu to appear
        - "<wait40>"
        # Navigate: Advanced options > Automated install
        - "<down><down><enter>"
        - "<wait><down><down><enter>"
        # Wait for installer to boot and prompt for preseed URL
        - "<wait70>"
        # Clear any pre-populated field value, then type the preseed URL
        - "<leftCtrlOn>a<leftCtrlOff>"
        - "<wait>"
        - "http://{{ .HTTPIP }}:{{ .HTTPPort }}/preseed.cfg"
        - "<enter>"
      provisioners:
        # Upload config files to the VM
        - name: upload-dhcp-network
          type: file
          file:
            fileRef: dhcp.network
            destination: /tmp/dhcp.network
        - name: upload-getty-script
          type: file
          file:
            fileRef: getty.sh
            destination: /tmp/getty.sh
        - name: upload-nsswitch-script
          type: file
          file:
            fileRef: nsswitch.sh
            destination: /tmp/nsswitch.sh
        # Upload ruddergrade binary
        - name: upload-ruddergrade
          type: file
          file:
            fileRef: ruddergrade
            destination: /tmp/ruddergrade
        # Configure systemd-networkd, serial console, and nsswitch
        - name: configure-system
          type: shell
          shell:
            executeCommand: "echo 'skills' | sudo -S sh -c '{{ .Command }}'"
            inline:
              - mv /etc/network/interfaces /etc/network/interfaces.save
              - mv /tmp/dhcp.network /etc/systemd/network/dhcp.network
              - mv /tmp/ruddergrade /bin/ruddergrade
              - chmod +x /bin/ruddergrade
              - systemctl enable --now systemd-networkd
              - bash /tmp/getty.sh
              - bash /tmp/nsswitch.sh
        # Trim disk to reduce image size
        # NOTE: These MUST be a single inline entry so the dd zero-fill
        # doesn't prevent uploading the next script (disk is full).
        - name: trim-disk
          type: shell
          shell:
            executeCommand: "echo 'skills' | sudo -S sh -c '{{ .Command }}'"
            inline:
              - |
                rm -rf /var/lib/apt/lists/*
                dd if=/dev/zero of=/EMPTY bs=1M || true
                rm -f /EMPTY
                sync
  timeout: 90m
